version: "3.9"

services:
  inframind:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inframind-monitor
    restart: unless-stopped
    ports:
      - "8050:8050"
    environment:
      APP_ENV: production
      TZ: UTC
    volumes:
      - inframind_data:/app/data

volumes:
  inframind_data:
